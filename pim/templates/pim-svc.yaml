apiVersion: v1
kind: Service
apiVersion: v1
metadata:
  name: pim-http
  labels:
    {{ include "pim.extravarsPim" . | indent 4}}
spec:
  type: NodePort
  selector:
    component: {{ template "pim.fullName" . }}
    role: pim-web
  ports:
    - port: 80
