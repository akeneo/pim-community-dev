parameters:
    pimee_product_asset.event_subscriber.class: PimEnterprise\Bundle\ProductAssetBundle\Event\AssetEventSubscriber

services:
    pimee_product_asset.event_subscriber:
        class: %pimee_product_asset.event_subscriber.class%
        arguments:
            - '@pimee_product_asset.command_launcher'
        tags:
            - { name: kernel.event_subscriber }
