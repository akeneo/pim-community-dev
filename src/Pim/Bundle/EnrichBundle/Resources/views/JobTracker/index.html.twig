{% extends 'PimEnrichBundle::layout.html.twig' %}
{% import 'PimDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}

    {{ elements.page_header("job_tracker.tab.title"|trans) }}

    {{ dataGrid.renderStatefulGrid('job-tracker-grid', { localeCode: locale_code() }) }}

{% endblock %}
