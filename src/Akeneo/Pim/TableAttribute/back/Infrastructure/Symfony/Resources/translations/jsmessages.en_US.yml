pim_table_attribute:
    form:
        attribute:
            tab:
                table_structure: Table structure
            table_attribute: Table attribute
            add_column: Add new columns
            select_type: Select your type
            data_type: Column type
            labels: Label translations
            column_management: Column management
            please_type: Please type "{{ text }}"
            confirm_column_delete: Are you sure you want to delete this column?
            confirm_option_delete: Are you sure you want to delete this option?
            empty_title: Your table options are empty.
            empty_subtitle: The first step is to define your columns.
            unique_title: Your table attribute is almost ready.
            unique_subtitle: Define at least 2 columns to start using your table.
            new_option_placeholder: Write your new option label
            manage_options: Manage options
            column_code: Column code
            properties_helper: You must define the table structure and at least 2 columns before you can save your table attribute
            limit_option_reached: You reached the limit of {{ limit }} options.
            template_label: Select the standard of your "{{ templateLabel }}" table
            delete_column_helper: When you delete this column, all of its values are removed from all the products using the "{{ attributeLabel }}" table attribute.
            delete_first_option_helper: When you delete this option, the entire row will be deleted for all the products using the "{{ attributeLabel }}" table attribute.
            delete_other_option_helper: When you delete this option, the cell value will be removed for all the products using the "{{ attributeLabel }}" table attribute.
            no_options_helper: Please add your first option.
            no_options: No options found
            please_try_again: Please try again
        product:
            items_per_page: Items per page
            current_items: "{{ minItem }}-{{ maxItem }} of {{ itemCount }}"
            current_page: "Page {{ currentPage }} / {{ pageCount }}"
            no_search_result: "Sorry, there are no lines for your search!"
            no_rows_title: "Your {{ attributeLabel }} table is empty."
            no_rows_subtitle: Add rows to define new properties
            no_add_options_title: To add rows
            no_add_options: Manage your "{{ attributeLabel }}" options
            no_add_options_unallowed: Contact your PIM administrator to manage the "{{ attributeLabel }}" options
            no_add_options_link: here
            no_edit_permission: No edit permission
    properties:
        data_type:
            text: Text
            number: Number
            boolean: Yes/No
            select: Select
    validations:
        column_code_must_be_filled: The code must be filled
        column_data_type_must_be_filled: The data type must be filled
        invalid_column_code: The column code can only contain letters, numbers and underscores
        invalid_code: The code can only contain letters, numbers and underscores
        duplicated_column_code: Each column requires a unique code. {{ duplicateCode }} is already used
        duplicated_select_code: Each code requires a unique code
        max_greater_than_min: Max should be greater than min
        max_length: Maximum number of characters
        min: Minimum value
        max: Maximum value
        decimals_allowed: Decimals values allowed
    templates:
        choose_template: Choose your template
        empty_table: Empty table
    product_edit_form:
        add_rows: Add rows
        search: Search
pim_enrich:
    entity:
        attribute:
            property:
                type:
                    pim_catalog_table: Table
batch_jobs:
    csv_table_attribute_options_export.label: Table attribute select options export in CSV
    xlsx_table_attribute_options_export.label: Table attribute select options export in XLSX
    csv_table_attribute_options_import.label: Table attribute select options import in CSV
    xlsx_table_attribute_options_import.label: Table attribute select options import in XLSX
