pimee_product_asset_index:
    path: /
    defaults: { _controller: pimee_product_asset.controller.product_asset:indexAction }

pimee_product_asset_view:
    path: /{id}/view
    defaults: { _controller: pimee_product_asset.controller.product_asset:viewAction }
    requirements:
        id: '[0-9]+'

pimee_product_asset_edit:
    path: /{id}/edit
    defaults: { _controller: pimee_product_asset.controller.product_asset:editAction }
    requirements:
        id: '[0-9]+'

pimee_product_asset_update:
    path: /{id}/update
    defaults: { _controller: pimee_product_asset.controller.product_asset:updateAction }
    requirements:
        id: '[0-9]+'

pimee_product_asset_upload_reference:
    path: /{assetId}/uploadReference/{id}
    defaults: { _controller: pimee_product_asset.controller.product_asset:uploadReferenceAction }
    requirements:
        assetId: '[0-9]+'
        id: '[0-9]+'

pimee_product_asset_upload_variation:
    path: /{assetId}/uploadVariation/{id}
    defaults: { _controller: pimee_product_asset.controller.product_asset:uploadVariationAction }
    requirements:
        assetId: '[0-9]+'
        id: '[0-9]+'

pimee_product_asset_rest_index:
    path: /rest/
    defaults: { _controller: pimee_product_asset.controller.rest.product_asset:indexAction, _format: json }
    methods: [GET]
