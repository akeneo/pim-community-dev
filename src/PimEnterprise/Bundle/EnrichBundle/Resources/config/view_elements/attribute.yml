parameters:
    pim_enrich.attribute.general_parameters.allowed_form_types:
        - code
        - attributeType
        - scopable
        - reference_data_name
        - localizable
        - unique
        - availableLocales
        - isReadOnly
