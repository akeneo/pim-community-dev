version: "3.9"
services:
  foo-api:
    image: wiremock/wiremock:2.34.0
    ports:
      - "8080:8080"
    command:
      - "--port=8080"
      - "--local-response-templating"
      - "--root-dir=/home/wiremock"
    volumes:
      - ../../infra/k8s/charts/wiremock/mappings:/home/wiremock/mappings
