{% macro time(date) -%}
    <time datetime="{{ date.format(constant('DateTime::ISO8601')) }}">{{ date | oro_format_time }}</time>
{%- endmacro %}

{% macro date(date) -%}
    <time datetime="{{ date.format(constant('DateTime::ISO8601')) }}">{{ date | oro_format_date }}</time>
{%- endmacro %}

{% macro datetime(date) -%}
    <time datetime="{{ date.format(constant('DateTime::ISO8601')) }}">{{ date | oro_format_datetime }}</time>
{%- endmacro %}
