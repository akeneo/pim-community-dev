apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: standard-retain-csi
provisioner: pd.csi.storage.gke.io
parameters:
  type: pd-standard
reclaimPolicy: Retain
allowVolumeExpansion: true
volumeBindingMode: Immediate
