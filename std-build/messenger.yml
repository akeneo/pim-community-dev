framework:
    messenger:
        transports:
            webhook:
                dsn: doctrine://default
                options:
                    table_name: messenger_messages
                    auto_setup: false (ou un truc comme ça)
                serializer: pim_enrich.messenger.serializer.business_event
        routing:
            'Akeneo\Platform\Component\EventQueue\BusinessEvent': webhook
