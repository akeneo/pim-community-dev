pim_table_attribute:
    form:
        attribute:
            tab:
                table_structure: Table structure
            table_attribute: Table attribute
            add_column: Add a new column
            select_type: Select your type
            data_type: Data type
            labels: Labels
            columns: Columns
            characters_left: ]-Inf, 1]{{ count }} character left|]1, Inf[{{ count }} characters left
            please_type: Please type "{{ text }}"
            confirm_column_delete: Are you sure you want to delete this column?
            confirm_option_delete: Are you sure you want to delete this option?
            empty_title: Your table options are empty!
            empty_subtitle: Add new columns to define your table composition
            unique_title: Your table attribute is almost ready!
            unique_subtitle: You need 2 columns minimum for your attribute to be valid.
            new_option_placeholder: Write your new option label
            search_placeholder: Write your new option label
            manage_options: Manage options
        product:
            items_per_page: Items per page
            current_items: "{{ minItem }}-{{ maxItem }} of {{ itemCount }}"
            current_page: "Page {{ currentPage }} / {{ pageCount }}"
            no_search_result: "Sorry, there are no lines for your search!"
            no_rows_title: "Your {{ attributeLabel }} is empty"
            no_rows_subtitle: "TODO To add lines, define new properties"
    properties:
        data_type:
            text: Text
            number: Number
            boolean: Boolean
            select: Select
    validations:
        column_code_must_be_filled: The code must be filled
        column_data_type_must_be_filled: The data type must be filled
        invalid_code: The column code can only contain letters, numbers and underscores
        duplicated_column_code: Each column requires a unique code. {{ duplicateCode }} is already used
        duplicated_select_code: Each code requires a unique code
        max_greater_than_min: Max should be greater than min
        max_length: Max characters
        min: Min number
        max: Max number
        decimals_allowed: Decimals values allowed
    templates:
        choose_template: Choose your template
        empty_table: Empty table
    product_edit_form:
        add_rows: Add rows
        search: Search
pim_enrich:
    entity:
        attribute:
            property:
                type:
                    pim_catalog_table: Table
batch_jobs:
    csv_table_attribute_options_export.label: Table attribute select options export in CSV
    xlsx_table_attribute_options_export.label: Table attribute select options export in XLSX
