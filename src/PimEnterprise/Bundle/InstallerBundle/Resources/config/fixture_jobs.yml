jobs:
    fixtures_attribute_group_access_csv:
        order: 150
        connector: Data fixtures
        alias:     fixtures_attribute_group_access_csv
        label:     Attribute group accesses data fixtures
        type:      fixtures
        configuration:
            filePath: attribute_group_accesses.csv

    fixtures_product_category_access_csv:
        order: 150
        connector: Data fixtures
        alias:     fixtures_product_category_access_csv
        label:     Product category accesses data fixtures
        type:      fixtures
        configuration:
            filePath: product_category_accesses.csv

    fixtures_asset_category_csv:
        order: 30
        connector: Data fixtures
        alias:     fixtures_asset_category_csv
        label:     Asset categories data fixtures
        type:      fixtures
        configuration:
            filePath: asset_categories.csv
            uploadAllowed: true
            delimiter: ;
            enclosure: '"'
            escape: '\'

    fixtures_asset_category_access_csv:
        order: 170
        connector: Data fixtures
        alias:     fixtures_asset_category_access_csv
        label:     Asset category accesses data fixtures
        type:      fixtures
        configuration:
            filePath: asset_category_accesses.csv

    fixtures_job_profile_access_csv:
        order:     150
        connector: Data fixtures
        alias:     fixtures_job_profile_access_csv
        label:     Job profile accesses data fixtures
        type:      fixtures
        configuration:
            filePath: job_profile_accesses.csv

    fixtures_locale_access_csv:
        order:     30
        connector: Data fixtures
        alias:     fixtures_locale_access_csv
        label:     Locale accesses data fixtures
        type:      fixtures
        configuration:
            filePath: locale_accesses.csv

    fixtures_rule_yml:
        order:     70
        connector: Data fixtures
        alias:     fixtures_rule_yml
        label:     rules data fixtures
        type:      fixtures
        configuration:
            filePath: rules.yml

    fixtures_asset_csv:
        order:     70
        connector: Data fixtures
        alias:     fixtures_asset_csv
        label:     assets data fixtures
        type:      fixtures
        configuration:
            filePath: assets.csv

    fixtures_asset_channel_configurations_yml:
        order:     110
        connector: Data fixtures
        alias:     fixtures_asset_channel_configurations_yml
        label:     assets data fixtures
        type:      fixtures
        configuration:
            filePath: asset_channel_configurations.yml
