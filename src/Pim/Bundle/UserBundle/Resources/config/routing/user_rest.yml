pim_user_user_rest_get_current:
    path: /{_format}
    defaults: { _controller: pim_user.controller.user_rest:getCurrentAction, _format: json }
    requirements:
        _format: json
        method: GET

pim_user_user_rest_get:
    path: /{identifier}
    defaults: { _controller: pim_user.controller.user_rest:getAction, _format: json }
    requirements:
        _format: json
        method: GET
