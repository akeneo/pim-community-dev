pim_enrich_job_execution_rest_get:
    path: /rest/{identifier}
    defaults: { _controller: pim_enrich.controller.rest.job_execution:getAction }
    methods: [GET]

pim_enrich_job_execution_progress_rest_get:
    path: /rest/{jobId}/progress
    defaults: { _controller: pim_enrich.controller.rest.job_execution_progress:getAction }
    methods: [ GET ]
