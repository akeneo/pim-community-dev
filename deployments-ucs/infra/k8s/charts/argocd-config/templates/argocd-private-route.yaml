apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: argocd-private
  namespace: argocd
spec:
  parentRefs:
  - name: internal-http
    namespace: default
  hostnames:
  - "{{ .Values.argocdConfig.privateHostname }}"
  rules:
  - matches:
    - path:
        value: /
    backendRefs:
    - name: argocd-server
      port: 80
