pim_enrich:
    # Locale
    locale:
        tab:
            property.title: Properties

    channel:
        flash:
            delete:
                error: You can not remove channel "%channelCode%" because your PIM must contain at least one channel.
                linked_to_export_profile: Impossible to delete a channel linked to an export profile.
