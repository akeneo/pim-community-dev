{% extends 'PimEnrichBundle::layout.html.twig' %}
{% import 'PimDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}
    {% set title = 'proposal.overview'|trans %}

    {{ elements.page_header(title) }}

    {{ dataGrid.renderGrid('proposal-grid', { dataLocale: locale_code(), currentUser: app.user }) }}
{% endblock %}
