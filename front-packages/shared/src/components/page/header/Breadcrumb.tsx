import styled from 'styled-components';

const Breadcrumb = styled.div`
  flex-grow: 1;
  max-height: 32px;
  overflow: hidden;
  margin-right: 20px;
  line-height: 16px;
  white-space: nowrap;
  text-overflow: ellipsis;
`;
export {Breadcrumb};
