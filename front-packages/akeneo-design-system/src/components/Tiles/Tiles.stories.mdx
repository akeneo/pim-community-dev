import {Meta, Story, ArgsTable, Canvas} from '@storybook/addon-docs/blocks';
import {Tiles, Tile} from './Tiles.tsx';
import {
  AssetCollectionIcon,
  DateIcon,
  FileIcon,
  AttributeNumberIcon,
  AttributeTextIcon,
  IdIcon,
  MetricIcon,
  AssetsIcon,
} from '../../icons';
import {useTabBar} from "../../../lib/hooks/useTabBar";

<Meta
  title="Components/Tiles"
  component={Tiles}
  argTypes={{
    size: {control: {type: 'select', options: ['small', 'big']}},
  }}
  args={{size: 'small'}}
/>

# Tiles

## Usage

The Tiles component provides the user a list of choices, for example, an attribute type, a template, or an export format.
It is a visual component made up of an icon and a label.

### Format

The component is displayed in a configurable responsive grid (width and gap). Tiles is a set of Tile.

## Playground

<Canvas>
  <Story name="Standard">
    {args => {
      const [isCurrent, switchTo] = useTabBar('Identifier');
      return <Tiles {...args} >
        <Tile selected={isCurrent('Assets collection')} onClick={() => {switchTo('Assets collection')}}>
          <AssetCollectionIcon/>
          Assets collection
        </Tile>
        <Tile selected={isCurrent('Date')} onClick={() => {switchTo('Date')}}>
          <DateIcon/>
          Date
        </Tile>
        <Tile selected={isCurrent('File')} onClick={() => {switchTo('File')}}>
          <FileIcon/>
          File
        </Tile>
        <Tile selected={isCurrent('Identifier')} onClick={() => {switchTo('Identifier')}}>
          <IdIcon/>
          Identifier
        </Tile>
        <Tile selected={isCurrent('Image')} onClick={() => {switchTo('Image')}}>
          <AssetsIcon/>
          Image
        </Tile>
        <Tile selected={isCurrent('Measurement')} onClick={() => {switchTo('Measurement')}}>
          <MetricIcon/>
          Measurement
        </Tile>
        <Tile selected={isCurrent('Text')} onClick={() => {switchTo('Text')}}>
          <AttributeTextIcon/>
          Text
        </Tile>
        <Tile selected={isCurrent('Number')} onClick={() => {switchTo('Number')}}>
          <AttributeNumberIcon/>
          Number
        </Tile>
      </Tiles>;
    }}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

## Variation on size

<Canvas>
  <Story name="Size">
    {args => {
      return (
        <>
          <Tiles size={'small'}>
            <Tile>
              <AssetCollectionIcon/>
              Small Assets collection
            </Tile>
            <Tile>
              <DateIcon/>
              Small Date
            </Tile>
          </Tiles>
          <Tiles size={'big'}>
            <Tile>
              <AssetCollectionIcon/>
              Big Assets collection
            </Tile>
            <Tile>
              <DateIcon/>
              Big Date
            </Tile>
          </Tiles>
        </>
      );
    }}
  </Story>
</Canvas>
