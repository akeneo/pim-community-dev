{
    "attr": {"id":"node_{{ category.id }}"},
    "data": "{{ category.label|raw }}{% if product_count %} ({{ count_products(category, nested) }}){% endif %}",
    "state": "{{ category|state }}"
    {% if children is defined %},"children": {{ children }}{% endif %}
}
