import type {AkeneoThemedProps, Color, FontFamily, FontSize, Level, Palette, Theme} from './theme';
import {getColor, getColorForLevel, getFontFamily, getFontSize} from './theme';
import {
  BrandedPath,
  color,
  CommonStyle,
  fontFamily,
  fontSize,
  palette,
  placeholderStyle,
  SkeletonPlaceholder,
} from './common';

export type {AkeneoThemedProps, Theme, FontSize, FontFamily, Color, Level, Palette};
export {
  getColor,
  getFontFamily,
  getColorForLevel,
  getFontSize,
  color,
  fontFamily,
  fontSize,
  palette,
  CommonStyle,
  BrandedPath,
  SkeletonPlaceholder,
  placeholderStyle,
};
