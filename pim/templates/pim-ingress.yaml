apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: pim-external-{{ .Values.global.extraLabels.pfid }}
  labels:
    app: {{ template "pim.fullName" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    component: {{ template "pim.fullName" . }}
    role: pim-web
    {{ include "pim.extravarsPim" . | indent 4}}
  annotations:
    kubernetes.io/ingress.class: traefik
    certmanager.k8s.io/cluster-issuer: {{ .Values.certmanager.issuer }}
spec:
  tls:
  - hosts:
    - {{ required ".Values.common.pimMasterUrl" .Values.common.pimMasterUrl }}
    secretName: {{ .Values.certmanager.issuer }}
  rules:
  - host: {{ required ".Values.common.pimMasterUrl" .Values.common.pimMasterUrl }}
    http:
      paths:
      - path: /
        backend:
          serviceName: pim-http
          servicePort: pim-http
