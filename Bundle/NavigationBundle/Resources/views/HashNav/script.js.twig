<script type="text/javascript">
    $(function() {
        if (Oro.hashNavigationEnabled()) {
            Oro.hashNavigationInstance = new Oro.Navigation({baseUrl : '{{ app.request.getSchemeAndHttpHost() }}'});
            if (!Backbone.History.started) {
                Backbone.history.start();
            }
        }
    })
</script>
