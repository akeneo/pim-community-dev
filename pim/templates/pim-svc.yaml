apiVersion: v1
kind: Service
apiVersion: v1
metadata:
  name: pim-http
  labels:
    {{ include "pim.extravarsPim" . | indent 4}}
    role: pim-web
spec:
  type: ClusterIP
  selector:
    component: {{ template "pim.fullName" . }}
    role: pim-web
  ports:
    - name: pim-http
      port: 80
