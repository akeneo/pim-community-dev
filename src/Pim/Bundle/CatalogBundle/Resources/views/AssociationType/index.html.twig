{% extends 'PimCatalogBundle::layout.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}

    {% set title = 'association type.overview'|trans %}

    {% set buttons %}
        {{ elements.createBtn(
            'association type',
            path('pim_catalog_association_type_create'),
            'dialog',
            'pim_catalog_association_type_create'
        ) }}
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    {{ dataGrid.renderGrid('association-type-grid', { localeCode: localeCode }) }}

{% endblock %}
