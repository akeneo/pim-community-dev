import styled from 'styled-components';
import {PageContent} from '@akeneo-pim-community/shared';

const CategoryPageContent = styled(PageContent)`
  padding: 0 40px 40px;
`;

CategoryPageContent.displayName = 'CategoryPageContent';
export {CategoryPageContent};
