{% extends 'PimEnrichBundle::layout.html.twig' %}
{% import 'PimDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}

    {% set title = 'association type.overview'|trans %}

    {% set buttons %}
        {{ elements.createBtn(
            'association type',
            path('pim_enrich_associationtype_create'),
            'dialog',
            'pim_enrich_associationtype_create',
            'plus'
        ) }}
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    {{ dataGrid.renderStatefulGrid('association-type-grid', { localeCode: locale_code() }) }}

{% endblock %}
