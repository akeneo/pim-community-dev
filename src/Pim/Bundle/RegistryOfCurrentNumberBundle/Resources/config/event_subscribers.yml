parameters:
    pim_registry_current_number.event_subscriber.registry_current_attribute_number.class: Pim\Bundle\RegistryOfCurrentNumberBundle\EventSubscriber\RegistryOfCurrentAttributeNumberSubscriber

services:
    # Subscribers
    pim_registry_current_number.event_subscriber.registry_current_attribute_number:
        class: '%pim_registry_current_number.event_subscriber.registry_current_attribute_number.class%'
        arguments:
            - '@pim_registry_current_number.manager.registry_current_number'
        tags:
            - { name: kernel.event_subscriber }
