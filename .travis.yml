language: php
php:
    - "5.4"
    - "5.5"
    - "hhvm"

matrix:
    allow_failures:
        - php: "hhvm"

before_script:
    - echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
    - echo "extension=mongo.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
    - php -r "readfile('https://getcomposer.org/installer');" | php
    - hhvm composer.phar --no-update doctrine/mongodb-odm v1.0.0-beta10@dev
    - hhvm composer.phar --no-update doctrine/mongodb-odm-bundle v3.0.0-BETA6@dev
    - hhvm composer.phar --prefer-dist --no-scripts --no-interaction

script:
    - ./bin/phpunit -c app/phpunit.travis.xml --testsuite PIM_Unit_Test
    - ./phpspec-fix
