monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: warning
            handler:      nested
            buffer_size:  1000
        nested:
            type:  stream
            path:  "php://stderr"
            level: warning
            formatter: 'monolog.formatter.json'
        console:
            type:  console
