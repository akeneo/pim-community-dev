<script type="text/javascript">
'use strict';

$(function() {
    var createBtn = $('<button>', { id: 'tree-create' }).html(
        $('<i>', { 'class': 'icon-plus-sign' })
    ).on('click', function() {
        Pim.navigate('{{ path("pim_catalog_categorytree_create") }}');
    });

    // Instantiate sidebar
    $('#category-tree-container').sidebarize({ buttons: [ createBtn ] });
});
</script>
