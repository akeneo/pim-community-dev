{% if is_granted(constant('PimEnterprise\\Component\\Security\\Attributes::EDIT_ITEMS'), dataLocale) %}
    {{ elements.createBtn(
        'product',
        null,
        { 'class': 'create-product' },
        'pim_enrich_product_create',
        'plus'
    ) }}
{% endif %}
