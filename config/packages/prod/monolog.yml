monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: info
            handler:      nested
            buffer_size:  1000
        nested:
            type:  stream
            path:  "php://stderr"
            formatter: 'monolog.formatter.json'
            channels: ['!security']
            bubble: false
