{% macro createBtn(text, url, id, attr) %}
{% spaceless %}
    <a{% if id %} id="{{ id }}"{% endif %} class="btn btn-primary pull-right{% if attr.class is defined %} {{ attr.class }}{% endif %}" href="{{ url ? url : 'javascript:void(0);' }}" title="{{ text|trans }}"{% if attr %}{% for key, value in attr %}{% if key != 'class' %} {{ key }}="{{ value }}"{% endif %}{% endfor %}{% endif %}>{{ text|trans }}</a>
{% endspaceless %}
{% endmacro %}

{% macro submitBtn(text, icon) %}
{% spaceless %}
    {% set text = text ? text : 'Save' %}
    <button type="submit" class="btn btn-primary" title="{{ text|trans }}">
        {% if icon %}
            <i class="icon-{{ icon }}"></i>
        {% endif %}
        {{ text|trans }}
    </button>
{% endspaceless %}
{% endmacro %}

{% macro backLink(url, title) %}
{% spaceless %}
    {% set title = title ? title : 'Back to grid' %}
    <a class="btn icons-holder back-link" href="{{ url }}" title="{{ title|trans }}"><i class="icon-chevron-left"></i></a>
{% endspaceless %}
{% endmacro %}

{% macro deleteLink(url, message, title) %}
{% spaceless %}
    {% set message = message ? message : 'Are you sure you want to delete this item?' %}
    {% set title = title ? title : 'Delete confirmation' %}
    <a class="btn icons-holder-text no-hash" href="javascript:void(0);" title="{{ 'Delete'|trans }}" data-dialog="confirm" data-title="{{ title|trans }}" data-message="{{ message|trans }}" data-url="{{ url }}" data-method="DELETE">
        <i class="icon-trash"></i>
        {{ 'Delete'|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link(text, url, opts = {}) %}
{% spaceless %}
    {% set title = opts.title is defined ? opts.title : text %}
    {% set icon = opts.icon is defined ? opts.icon : null %}
    <a{% if opts.id is defined %} id="{{ opts.id }}"{% endif %} class="btn {{ opts.class is defined ? opts.class|join(' ') }}{% if text and icon %} icons-holder-text{% elseif icon %} icons-holder{% endif %}" href="{{ url ? url : 'javascript:void(0);' }}"{% if title %} title="{{ title|trans }}"{% endif %}{% if opts.attr is defined %}{% for key, value in opts.attr %} {{ key }}="{{ value }}"{% endfor %}{% endif %}>
        {% if icon %}<i class="icon-{{ icon }}"></i>{% endif %}
        {{ text|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro page_header(title, buttons, subtitle, left, right) %}
    <div class="navigation clearfix navbar-extra">
        <div class="row-fluid">
            {% if buttons is not empty %}
                <div class="navbar-buttons pull-right">
                    {{ buttons }}
                </div>
            {% endif %}
            <div class="navbar-content pull-left">
                <div class="navbar-title clearfix-oro">
                    <div class="sub-title">{{ title }}</div>
                    {% if subtitle is not empty %}
                        <span class="separator">/</span>
                        <div class="sub-title">{{ subtitle }}</div>
                    {% endif %}
                </div>
                {{ left }}
           </div>
            {{ right }}
        </div>
    </div>
{% endmacro %}

{% macro form_navbar(tabs) %}
    <div id="form-navbar" class="navbar navbar-static scrollspy-nav">
        <div class="navbar-inner">
            <div class="row-fluid">
                {% if tabs is not empty %}
                    <ul class="nav">
                        {% if tabs is iterable %}
                            {% for tab in tabs %}
                                <li{% if loop.first %} class="active"{% endif %}><a href="#{{ tab|replace({' ': '-', '.': '-'})|lower }}" data-toggle="tab">{{ tab|trans }}</a></li>
                            {% endfor %}
                        {% else %}
                            <li class="active"><a href="#{{ tabs|replace({' ': '-', '.': '-'})|lower }}" data-toggle="tab">{{ tabs|trans|capitalize }}</a></li>
                        {% endif %}
                    </ul>
                {% endif %}
            </div>
        </div>
    </div>
{% endmacro %}

{% macro form_errors(form) %}
    {% if form.vars.errors|length %}
        <div class="alert alert-error fade in top-messages">
            <a class="close" data-dismiss="alert" href="javascript:void(0);">×</a>
            <div class="message">
                {{ form_errors(form) }}
            </div>
        </div>
    {% endif %}
{% endmacro %}

{% macro accordion(items = {}, counter = 1) %}
    <div class="accordion">
        {% for title, content in items %}
            <div class="accordion-group">
                <div class="accordion-heading">
                    <a class="accordion-toggle" data-toggle="collapse" href="#collapse-{{ counter }}-{{ loop.index }}">
                        <i class="icon-collapse-alt"></i>
                        {{ title|trans }}
                    </a>
                </div>
                <div id="collapse-{{ counter }}-{{ loop.index }}" class="accordion-body in">
                    <div class="accordion-inner">
                        {{ content|raw }}
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endmacro %}
