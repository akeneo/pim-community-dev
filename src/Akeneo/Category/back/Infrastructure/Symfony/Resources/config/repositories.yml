services:
  pim_catalog.repository.category:
    class: 'Akeneo\Category\back\Infrastructure\Doctrine\CategoryRepository'
    factory: ['@doctrine.orm.entity_manager', 'getRepository']
    arguments: ['%pim_catalog.entity.category.class%']
    tags:
      - { name: 'pim_repository' }
