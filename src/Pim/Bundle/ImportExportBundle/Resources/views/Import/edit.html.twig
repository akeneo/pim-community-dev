{% if elements is not defined %}
    {% import 'PimUIBundle:Default:page_elements.html.twig' as elements %}
{% endif %}

{% set actionRoute = form.vars.value.id ?
    path('pim_importexport_import_edit', { 'id': form.vars.value.id }) :
    path('pim_importexport_import_create', { 'connector': connector, 'alias': alias }) %}

{% if form.vars.value.id %}
    {% set title = elements.title('import profile', 'edit', form.vars.value.label) %}
{% else %}
    {% set title = elements.title('import profile', 'create') %}
{% endif %}

{% set indexRoute = path('pim_importexport_import_index') %}
{% if form.vars.value.id %}
    {% set removeRoute = path('pim_importexport_import_remove', { 'id': form.vars.value.id }) %}
    {% set removeMessage = 'Are you sure you want to delete the import profile %import% ?'|trans({'%import%': form.vars.value.label}) %}
{% endif %}

{% include 'PimImportExportBundle:JobInstance:edit.html.twig' %}
