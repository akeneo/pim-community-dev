version: '3'

services:
    mysql-behat:
        tmpfs:
            - '/tmp'
            - '/var/lib/mysql'

    fpm:
        environment:
            BEHAT_SCREENSHOT_PATH: '/srv/pim/var/tests/screenshots'

    node:
        environment:
            YARN_REGISTRY: "http://registry.yarnpkg.com"
