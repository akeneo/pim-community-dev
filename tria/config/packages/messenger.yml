framework:
  messenger:
    default_bus: messenger.bus.default
    buses:
      messenger.bus.default: ~

      catalogs.command.bus:
        middleware:
          - validation
          - doctrine_transaction
      catalogs.query.bus:
        middleware:
          - validation
