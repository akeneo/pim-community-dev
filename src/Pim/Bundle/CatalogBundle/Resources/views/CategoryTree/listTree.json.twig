[
{% for tree in trees %}
    {
        "id": {{ tree.id }},
        "label": "{{ tree.label|raw }}",
        "selected": "{{ tree.id == selectedTreeId ? 'true' : 'false' }}"
    }
    {% if not loop.last %},{% endif %}
{% endfor %}
]
