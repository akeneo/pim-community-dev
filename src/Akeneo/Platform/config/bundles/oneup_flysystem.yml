oneup_flysystem:
    adapters:
        asset_storage_adapter:
            local:
                directory: '%asset_storage_dir%'
    filesystems:
        asset_storage:
            adapter: asset_storage_adapter
            mount: assetStorage
