pim_identifier_generator:
    acl:
        view: 'View identifier generators'
        manage: 'Manage identifier generators'
    acl_group:
        identifier_generator: 'Identifier Generators'
  condition:
    fields:
      family: Family
      enabled: Status
    field_groups:
      system: System
