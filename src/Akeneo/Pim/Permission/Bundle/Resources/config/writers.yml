services:
    pimee_security.writer.database.asset_category_access:
        class: '%pim_connector.writer.database.class%'
        arguments:
            - '@pimee_security.saver.asset_category_access'
            - '@akeneo_storage_utils.doctrine.object_detacher'

    pimee_security.writer.database.locale_access:
        class: '%pim_connector.writer.database.class%'
        arguments:
            - '@pimee_security.saver.locale_access'
            - '@akeneo_storage_utils.doctrine.object_detacher'

    pimee_security.writer.database.job_profile_access:
        class: '%pim_connector.writer.database.class%'
        arguments:
            - '@pimee_security.saver.job_profile_access'
            - '@akeneo_storage_utils.doctrine.object_detacher'

    pimee_security.writer.database.product_category_access:
        class: '%pim_connector.writer.database.class%'
        arguments:
            - '@pimee_security.saver.product_category_access'
            - '@akeneo_storage_utils.doctrine.object_detacher'

    pimee_security.writer.database.attribute_group_access:
        class: '%pim_connector.writer.database.class%'
        arguments:
            - '@pimee_security.saver.attribute_group_access'
            - '@akeneo_storage_utils.doctrine.object_detacher'
