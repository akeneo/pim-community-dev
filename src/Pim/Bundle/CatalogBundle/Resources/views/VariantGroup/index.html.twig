{% extends 'PimCatalogBundle::layout.html.twig' %}

{% block head_script %}
    {{ parent() }}

    {% include 'OroGridBundle:Include:javascript.html.twig' with {'datagridView': datagrid, 'selector': '#variant-grid'} %}
    {% include 'OroGridBundle:Include:stylesheet.html.twig' %}
{% endblock %}

{% block content %}

    {% set title = 'variant group.overview'|trans %}

    {% set buttons %}
        {{ elements.createBtn(
            'variant group',
            null,
            { 'id': 'create-variant-group', 'data-form': 'dialog', 'data-form-url': path('pim_catalog_variant_group_create') }
        ) }}
        <script type="text/javascript">
            require(
                ['jquery', 'pim/dialogform'],
                function ($, DialogForm) {
                    'use strict';
                    $(function() {
                        new DialogForm('#create-variant-group');
                    });
                }
            );
        </script>
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    <div id="variant-grid"></div>

{% endblock %}
