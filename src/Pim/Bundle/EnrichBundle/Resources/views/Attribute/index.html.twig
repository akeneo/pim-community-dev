{% extends 'PimEnrichBundle::layout.html.twig' %}

{% block content %}
    <div id="settings-attributes">
    </div>
{% endblock %}

{% block head_script %}
    {{ parent() }}
    <script>
        require(
            [
                'jquery',
                'pim/form-builder',
                'pim/fetcher-registry'
            ],
            function (
                $,
                FormBuilder,
                FetcherRegistry
            ) {
                FetcherRegistry.initialize().done(function () {
                    FormBuilder.build('pim-attribute-index')
                        .then(function (form) {
                            form.setElement('#settings-attributes').render();
                        });
                });
            });
    </script>
{% endblock %}
