services:
  akeneo.job_automation.feature_flag.is_remote_storage_enabled:
    class: 'Akeneo\Platform\Bundle\FeatureFlagBundle\Configuration\EnvVarFeatureFlag'
    arguments:
      - '%env(bool:FLAG_JOB_AUTOMATION_REMOTE_STORAGE_ENABLED)%'
