version: 2.1

slack-fail-post-step: &slack-fail-post-step
  post-steps:
    - slack/status:
        channel: ci
        webhook: $SLACK_NIGHTLY_STATUS
        fail_only: true

slack-post-step: &slack-post-step
  post-steps:
    - slack/status:
        channel: ci
        webhook: $SLACK_NIGHTLY_STATUS
        fail_only: false

client-to-clone-srnt: &client-to-clone-srnt ["srnt-madeira", "srnt-pim-pce", "srnt-cto-pim", "srnt-iteshop"]

client-to-clone-grth: &client-to-clone-grth ["grth-sirius-staging"]
