version: '3.4'

services:
    php:
        environment:
            PUBSUB_EMULATOR_HOST: pubsub-emulator:8085
            COMPOSER_AUTH: $COMPOSER_AUTH

    fpm:
        environment:
            PUBSUB_EMULATOR_HOST: pubsub-emulator:8085

    mysql:
        tmpfs:
            - '/tmp'
            - '/var/lib/mysql'
