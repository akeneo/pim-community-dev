oneup_flysystem:
    adapters:
        catalog_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/file_storage/%kernel.environment%/catalog'
        jobs_storage_adapter:
            local:
                location: '%kernel.project_dir%/var/file_storage/%kernel.environment%/jobs'
        archivist_adapter:
            local:
                location: '%kernel.project_dir%/var/file_storage/%kernel.environment%/archive'
