<ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
        <li>
            {{ breadcrumb.label|raw }}{% if not loop.last %}<span class="divider">/</span>{% endif %}
        </li>
    {% endfor %}
</ul>
