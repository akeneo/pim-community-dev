import {AppcuesAnalytics} from '@akeneo-pim-community/shared';

const PimAppcuesAnalytics: AppcuesAnalytics = {
  appcuesTrack(_event: string, _properties?: object) {
    // track user actions in the application
  },
};

export = PimAppcuesAnalytics;
