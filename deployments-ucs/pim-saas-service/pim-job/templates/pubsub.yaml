apiVersion: pubsub.gcp.jet.crossplane.io/v1alpha1
kind: Topic
metadata:
 name: "{{ .Values.pim.pubsub.ucs_topic_job_schudeler }}"
spec:
 forProvider:
   name: "{{ .Values.pim.pubsub.ucs_topic_job_schudeler }}"
 providerConfigRef:
   name: provider-config-jet-gcp
---
apiVersion: pubsub.gcp.jet.crossplane.io/v1alpha1
kind: Subscription
metadata:
 name: {{ .Values.pim.pubsub.ucs_topic_job_schudeler }}
spec:
 forProvider:
   ackDeadlineSeconds: 600
   labels:
     subscription_type: "usc-scheduler"
   name: {{ .Values.pim.pubsub.ucs_topic_job_schudeler }}
   topic: {{ .Values.pim.pubsub.ucs_topic_job_schudeler }}
 providerConfigRef:
   name: provider-config-jet-gcp