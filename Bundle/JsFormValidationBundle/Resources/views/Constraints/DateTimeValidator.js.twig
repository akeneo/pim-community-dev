function DateTime(field, params)
{
    var value = field && field.nodeName ? {{ getJsFormElementValue('field') }} : field;

    if (isNotDefined(value)) {
        return true;
    }

    // TODO: use moment JS format for current locale
    var dateTimeFormat = 'MMM D, YYYY h:mm A';
    if (Moment(String(value), dateTimeFormat, true).isValid()) {
        return true;
    }

    return getComputeMessage(params.message);
}
