apiVersion: v1
kind: Service
apiVersion: v1
metadata:
  name: pim-http
  labels:
    {{ include "pim.extraLabels" . | indent 4 }}
spec:
  type: ClusterIP
  selector:
    component: {{ template "pim.fullName" . }}
    role: pim-web
  ports:
    - name: pim-http
      port: 80
