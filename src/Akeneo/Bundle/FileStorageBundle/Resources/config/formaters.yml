parameters:
    akeneo_file_storage.file_storage.formater.bytes.class: Akeneo\Component\FileStorage\Formater\BytesFormater

services:
    akeneo_file_storage.file_storage.formater.bytes:
        class: '%akeneo_file_storage.file_storage.formater.bytes.class%'
