parameters:
    pimee_dashboard.widget.proposal.class: PimEnterprise\Bundle\DashboardBundle\Widget\ProposalWidget

services:
    pimee_dashboard.widget.proposal:
        class: %pimee_dashboard.widget.proposal.class%
        arguments:
            - '@pim_catalog.doctrine.smart_manager_registry'
        tags:
            - { name: pim_dashboard.widget, alias: proposals }
