oro:
    entity_config:
        controller:
            config_entity:
                message:
                    saved: "Entity saved"
            config_field:
                message:
                    saved: "Field saved"
