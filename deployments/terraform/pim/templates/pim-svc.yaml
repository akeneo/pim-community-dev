---
apiVersion: v1
kind: Service
metadata:
  name: pim-http
  labels:
    {{- include "pim.extraLabels" . | indent 4 }}
spec:
  type: ClusterIP
  selector:
    component: pim-web
    role: pim-web
  ports:
    - name: pim-http
      port: 80
