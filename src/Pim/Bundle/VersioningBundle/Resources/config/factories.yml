parameters:
    pim_versioning.factory.version.class: Akeneo\Tool\Bundle\VersionningBundle\Factory\VersionFactory

services:
    pim_versioning.factory.version:
        class: '%pim_versioning.factory.version.class%'
        arguments:
            - '%pim_versioning.entity.version.class%'
