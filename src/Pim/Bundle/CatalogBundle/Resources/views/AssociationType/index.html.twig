{% extends 'PimCatalogBundle::layout.html.twig' %}

{% block head_script %}
    {{ parent() }}

    {% include 'OroGridBundle:Include:javascript.html.twig' with {'datagridView': datagrid, 'selector': '#association-type-grid'} %}
    {% include 'OroGridBundle:Include:stylesheet.html.twig' %}
{% endblock %}

{% block content %}

    {% set title = 'association type.overview'|trans %}

    {% set buttons %}
        {% if resource_granted('pim_catalog_association_type_create') %}
            {{ elements.createBtn(
                'association type',
                null,
                { 'id': 'create-association-type', 'data-form': 'dialog', 'data-form-url': path('pim_catalog_association_type_create') }
            ) }}
            <script type="text/javascript">
                require(
                    ['jquery', 'pim/dialogform'],
                    function ($, DialogForm) {
                        'use strict';
                        $(function() {
                            new DialogForm('#create-association-type');
                        });
                    }
                );
            </script>
        {% endif %}
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    <div id="association-type-grid"></div>

{% endblock %}
