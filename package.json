{
  "name": "pim-enterprise-dev",
  "version": "1.6.1",
  "description": "Akeneo PIM Enterprise Application",
  "homepage": "http://www.akeneo.com",
  "private": true,
  "scripts": {
    "install": "npm install vendor/akeneo/pim-community-dev",
    "webpack": "webpack --display-reasons --display-error-details --config webpack.config.js --progress --display-modules",
    "webpack-watch": "webpack --progress --config webpack.config.js --watch",
    "lint": "eslint 'src/**/*.js' --ignore-path .eslintignore --quiet",
    "lint-fix": "eslint 'src/**/*.js' --ignore-path .eslintignore --quiet --fix"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/akeneo/pim-enterprise-dev.git"
  },
  "bugs": {
    "url": "https://github.com/akeneo/pim-enterprise-dev/issues"
  }
}
