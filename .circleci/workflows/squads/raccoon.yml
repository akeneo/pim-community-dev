workflows:
  raccoon_tailored_import_pull_request:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - ready_to_test_tailored_import?:
          name: "Start tailored import?"
          type: approval
          filters:
            branches:
              only:
                - /RAB-.*/
                - /RAC-.*/
      - checkout:
          requires:
            - "Start tailored import?"
      - build_srnt_dev:
          name: "Build dev"
          requires:
            - checkout
      - test_helm_generated_k8s_files:
          name: "🤷 ça bouine"
          requires:
            - checkout
      - deploy_pr_environment?:
          name: "Deploy PR environment?"
          type: approval
          filters:
            branches:
              only:
                - /RAB-.*/
                - /RAC-.*/
      - build_srnt_prod:
          name: "Build prod"
          requires:
            - "🤷 ça bouine"
            - "Deploy PR environment?"
      - deploy_pr_environment:
          name: "Deploy PR environment"
          requires:
            - "Build prod"
      - delete_pr_environment?:
          name: "Delete PR environment?"
          type: approval
          requires:
            - "Deploy PR environment"
      - delete_pr_environment:
          name: "Delete PR environment"
          requires:
            - "Delete PR environment?"
      - test_front_tailored_import:
          name: "Tailored import front test"
          requires:
            - "Build dev"
      - test_back_tailored_import:
          name: "Tailored import back test"
          requires:
            - "Build dev"
      - pull_request_success:
          name: "PR Success"
          requires:
            - "Tailored import front test"
            - "Tailored import back test"

  raccoon_job_automation_pull_request:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - ready_to_test_job_automation?:
          name: "Start job automation?"
          type: approval
          filters:
            branches:
              only:
                - /RAB-.*/
                - /RAC-.*/
      - checkout:
          requires:
            - "Start job automation?"
      - build_srnt_dev:
          name: "Build dev"
          requires:
            - checkout
      - test_helm_generated_k8s_files:
          name: "🤷 ça bouine"
          requires:
            - checkout
      - deploy_pr_environment?:
          name: "Deploy PR environment?"
          type: approval
          filters:
            branches:
              only:
                - /RAB-.*/
                - /RAC-.*/
      - build_srnt_prod:
          name: "Build prod"
          requires:
            - "🤷 ça bouine"
            - "Deploy PR environment?"
      - deploy_pr_environment:
          name: "Deploy PR environment"
          requires:
            - "Build prod"
      - delete_pr_environment?:
          name: "Delete PR environment?"
          type: approval
          requires:
            - "Deploy PR environment"
      - delete_pr_environment:
          name: "Delete PR environment"
          requires:
            - "Delete PR environment?"
      - test_front_job_automation:
          name: "Job automation front test"
          requires:
            - "Build dev"
      - test_back_job_automation:
          name: "Job automation back test"
          requires:
            - "Build dev"
      - pull_request_success:
          name: "PR Success"
          requires:
            - "Job automation front test"
            - "Job automation back test"
