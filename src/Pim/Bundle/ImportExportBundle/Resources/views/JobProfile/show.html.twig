{% extends 'PimEnrichBundle::layout.html.twig' %}
{% import 'PimDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}
    {%set upload_is_allowed = uploadForm is defined and uploadForm is not empty and uploadAllowed and uploadViolations|length == 0 %}

    {% if not upload_is_allowed %}
        {{ form_start(form) }}
    {% endif %}

    {% include 'PimImportExportBundle:JobProfile:_show_headers.html.twig' with {'form': form} %}

    <div class="AknTabContainer">
        {{ elements.form_navbar(view_element_aliases(viewElementsAliases)) }}
        <div class="AknTabContainer-content">
            {% include 'PimImportExportBundle:JobProfile:_form.html.twig' %}
        </div>
    </div>

    {% if not upload_is_allowed %}
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
