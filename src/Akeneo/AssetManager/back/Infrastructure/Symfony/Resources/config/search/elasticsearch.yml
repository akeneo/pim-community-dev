parameters:
    record_index_configuration:
        - '%pim_ee_dev_src_folder_location%/src/Akeneo/ReferenceEntity/back/Infrastructure/Symfony/Resources/config/search/record_index_configuration.yml'

akeneo_elasticsearch:
    hosts: "%index_hosts%"
    indexes:
        -
            service_name: "akeneo_referenceentity.client.record"
            index_name: "%record_index_name%"
            configuration_files: "%record_index_configuration%"
