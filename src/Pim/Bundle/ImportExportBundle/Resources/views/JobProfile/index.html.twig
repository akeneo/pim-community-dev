{% extends 'PimImportExportBundle::layout.html.twig' %}

{% block head_script %}
    {{ parent() }}

    {% include 'OroGridBundle:Include:javascript.html.twig' with {'datagridView': datagrid, 'selector': '#job-grid'} %}
    {% include 'OroGridBundle:Include:stylesheet.html.twig' %}
{% endblock %}

{% block content %}

    <script type="text/javascript">
        require(
            ['jquery', 'pim/dialogform'],
            function ($, dialogForm) {
                'use strict';
                $(function() {
                    new dialogForm('#create-job');
                });
            }
        );
    </script>

    {% set buttons %}
        {{ elements.createBtn(
            'export',
            null,
            { 'id': 'create-job', 'data-form': 'dialog', 'data-form-url': path('pim_importexport_export_profile_create') }
        ) }}
    {% endset %}

    {{ elements.page_header((entityName ~ '.overview')|trans, buttons) }}

    <div id="job-grid"></div>

{% endblock %}
