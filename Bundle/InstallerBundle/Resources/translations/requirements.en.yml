settings:
    header:               PHP version and settings
    any:                  any
    timezone:             Timezone
    timezone_deprecated:
        header:           Deprecated timezone
        help:             Timezone %timezone% is deprcated. Fix your php.ini file (list of deprecated timezones http://php.net/manual/en/timezones.others.php)
    non_deprecated:       non deprecated
    version:              PHP version
    version_recommended:  Recommended PHP version
    memory_limit:         Memory limit

extensions:
    header:               Extensions
    accelerator:
        header:           Accelerator
        help:             Install and enable a PHP accelerator like APC (highly recommended)
    apc:                  APC
    help:                 Install and enable the %extension% extension
    iconv:                Iconv
    icu:                  ICU
    intl:                 Intl
    json_encode:          JSON
    mbstring:             Multibyte String
    pcre:                 PCRE
    pdo:                  PDO
    php_xml:              PHP-XML
    session_start:        Session
    simplexml_import_dom: SimpleXML
    token_get_all:        Tokenizer

filesystem:
    header:               File system
    cache:
        header:           Cache directory
        help:             "<![CDATA[To set permissions run: sudo setfacl -R -m u:www-data:rwX -m u:`whoami`:rwX %path% & sudo setfacl -dR -m u:www-data:rwx -m u:`whoami`:rwx %path%]]>"
    logs:
        header:           Logs directory
        help:             "<![CDATA[To set permissions run: sudo setfacl -R -m u:www-data:rwX -m u:`whoami`:rwX %path% & sudo setfacl -dR -m u:www-data:rwx -m u:`whoami`:rwx %path%]]>"
    exists:               exists
    not_exists:           not exists
    writable:             writable
    not_writable:         not writable
    parameters:
        header:           parameters.yml
        help:             "To set permissions run: sudo chmod a+w %path%"
    vendors:              Vendors directory
