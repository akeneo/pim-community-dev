parameters:
    pim_datagrid.product.attribute_type.akeneo_asset_multiple_link:
        column:
            type:         .asset_multiple_link
        filter:
            type:    .asset_multiple_link
            ftype:   .asset_multiple_link
            options:
                field_options:
                    multiple: true
                    attr:
                        empty_choice: true
        sorter: ~
