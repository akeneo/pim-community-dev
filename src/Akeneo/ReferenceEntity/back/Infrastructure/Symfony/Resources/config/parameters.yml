parameters:
    # Maximum number of reference entities
    reference_entity_limit: 100
    # Maximum number of attributes per reference entities
    reference_entity_maximum_attribute: 100
    # Maximum number of records for single reference entity
    reference_entity_record_limit_per_reference_entity: 1000000
    # Maximum number of option for a single list attribute of a reference entity
    reference_entity_option_limit_per_list_attribute: 100
    # Number of records per page for the connector
    reference_entity_connector_records_limit_per_page: 100
    # Number of reference entities per page for the connector
    reference_entity_connector_reference_entities_limit_per_page: 200
    # Maximum number of records to process in a single API request
    reference_entity_connector_maximum_records_to_process_per_request: 100
    # Set to "true" to automatically load reference entities fixtures
    env(SHOULD_LOAD_REFERENCE_ENTITIES_FIXTURES): 'false'
    should_load_reference_entities_fixtures: '%env(bool:SHOULD_LOAD_REFERENCE_ENTITIES_FIXTURES)%'
    average_max_number_of_values_per_record_limit: -1
