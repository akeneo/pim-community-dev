monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
            buffer_size:  1000
        nested:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
        event_api:
            type: "null"
