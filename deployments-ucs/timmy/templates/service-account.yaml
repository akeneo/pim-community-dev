apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.common.workloadIdentityKSA }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "timmy.labels" . | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: {{ include "timmy.deploymentGSAEmail" . }}
