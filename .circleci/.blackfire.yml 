tests:
    "TEST1":
        command: ".*"
        assertions:
            - "metrics.sql.queries.count > 1"

    "TEST2":
        path: "/.*"
        assertions:
            - "metrics.sql.queries.count < 1"
