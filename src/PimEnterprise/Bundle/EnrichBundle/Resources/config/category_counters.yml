parameters:
    pimee_enrich.doctrine.counter.granted_category_items.class: PimEnterprise\Bundle\EnrichBundle\Doctrine\Counter\GrantedCategoryItemsCounter

services:
    pimee_enrich.doctrine.counter.category_product:
        class: %pimee_enrich.doctrine.counter.granted_category_items.class%
        arguments:
            - '@pim_catalog.repository.product_category'
            - '@pim_catalog.repository.category'
            - '@pimee_security.repository.category_access'
            - '@security.authorization_checker'
            - '@security.token_storage'
        tags:
            - { name: pim_enrich.doctrine.counter.category_item, type: product }
