{% extends 'OroUIBundle:actions:update.html.twig' %}
{% form_theme form with 'OroUIBundle:Form:fields.html.twig' %}

{% set title = 'Add Field ' %}
{% oro_title_set({params : {"%username%": 'User' }}) %}

{% set formAction = path('oro_entityextend_field_create', {id: entity_id}) %}

{% block navButtons %}
    {{ UI.button({'path' : path('oro_entityconfig_view', {'id':entity_id}), 'title' : 'Cancel', 'label' : 'Cancel'}) }}
    {{ UI.buttonType({'type': 'submit', 'class': 'btn-success', 'label': 'Save'}) }}
{% endblock navButtons %}

{#{% block pageHeader %}#}
    {#{% set breadcrumbs = {#}
    {#'entity':      'entity',#}
    {#'indexPath':   path('oro_entityconfig_fields', {'id':entity_id}),#}
    {#'indexLabel': 'Fields',#}
    {#'entityTitle': 'Create Field',#}
    {#}#}
    {#%}#}
    {#{{ parent() }}#}
{#{% endblock pageHeader %}#}

{% block pageHeader %}
    {% set breadcrumbs = {} %}
    {{ parent() }}
{% endblock pageHeader %}

{% block breadcrumbs %}
    <div class="pull-left">
        <div class="sub-title"><a href="{{ path('oro_entityconfig_index') }}">{{ 'Entities'|trans }}</a></div>
        <span class="separator">/</span>
        <div class="sub-title"><a href="{{ path('oro_entityconfig_view', {id: entity_id}) }}">{{ entity_config.get('label') }}</a></div>
        <span class="separator">/</span>
        <h1 class="user-name">New Field</h1>
    </div>
{% endblock breadcrumbs %}


{% block stats %}
    {{ parent() }}
{% endblock stats %}

{% block content_data %}
    {% set id = 'configfield-create' %}

    {% set dataBlocks = form_data_blocks(form) %}

    {% set data = {
    'formErrors': form_errors(form)? form_errors(form) : null,
    'dataBlocks': dataBlocks,
    'hiddenData': form_rest(form)
    }%}

    {{ parent() }}
{% endblock content_data %}
