apiVersion: batch/v1
kind: Job
metadata:
  name: push-cloud-function
  labels:
    {{- include "timmy.labels" . | nindent 4 }}
    component: "push-cloud-function"
    app.kubernetes.io/component: "push-cloud-function"
  annotations:
    checksum/config: {{ include (print $.Template.BasePath "/configmap-sources.yaml") . | sha256sum }}
spec:
  template:
    metadata:
      labels:
        {{- include "timmy.labels" . | nindent 8 }}
        component: "push-cloud-function"
        app.kubernetes.io/component: "push-cloud-function"
      annotations:
        argocd.argoproj.io/sync-wave: "-1"
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ .Values.common.workloadIdentityKSA | quote }}
      nodeSelector:
        iam.gke.io/gke-metadata-server-enabled: "true"
      initContainers:
        - name: workload-identity-initcontainer
          image: "{{ .Values.image.cloudsdk.repository }}:{{ .Values.image.cloudsdk.tag }}"
          command:
            - '/bin/bash'
            - '-c'
            - |
              curl -s -H 'Metadata-Flavor: Google' 'http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token' --retry 30 --retry-connrefused --retry-max-time 30 > /dev/null || exit 1
      containers:
        - name:  push-cloud-function
          image: {{ .Values.image.curljqzip.repository }}:{{ .Values.image.curljqzip.tag }}
          imagePullPolicy: {{ .Values.image.curljqzip.pullPolicy | quote }}
          command: ["sh", "/scripts-dir/cloud-function-scripts.sh"]
          env:
            - name: BUCKET_NAME
              value: {{ include "timmy.bucketName" . }}
            - name: SOURCE_CODE_ZIP
              value: {{ .Values.cloudFunction.sourceCodeZip }}
          volumeMounts:
            - name: cloud-function-scripts
              mountPath: /scripts-dir
            - name: cloud-function-sources
              mountPath: /functions/portal/package.json
              subPath: package.json
            - name: cloud-function-sources
              mountPath: /functions/portal/index.js
              subPath: index.js
      volumes:
        - name: cloud-function-scripts
          configMap:
            name: cloud-function-scripts
        - name: cloud-function-sources
          configMap:
            name: cloud-function-sources
            items:
              - key: package.json
                path: package.json
              - key: index.js
                path: index.js
