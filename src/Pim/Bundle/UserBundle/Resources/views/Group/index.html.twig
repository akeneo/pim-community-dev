{% extends 'PimEnrichBundle::layout.html.twig' %}

{% block content %}
    <div id="system-user-group">
    </div>
{% endblock %}

{% block head_script %}
    {{ parent() }}
    <script>
        require(
            [
                'jquery',
                'pim/form-builder'
            ],
            function (
                $,
                FormBuilder
            ) {
                $(function () {
                    FormBuilder.build('pim-user-group-index')
                        .then(function (form) {
                            form.setElement('#system-user-group').render();
                        });
                })
            });
    </script>
{% endblock %}
