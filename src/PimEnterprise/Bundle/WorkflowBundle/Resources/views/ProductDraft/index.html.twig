{% extends 'PimEnrichBundle::layout.html.twig' %}

{% block content %}
    <div id="enrich-proposal">
    </div>
{% endblock %}

{% block head_script %}
    {{ parent() }}
    <script>
        require(
            [
                'jquery',
                'pim/form-builder',
                'pim/fetcher-registry'
            ],
            function (
                $,
                FormBuilder,
                FetcherRegistry
            ) {
                FetcherRegistry.initialize().done(function () {
                    FormBuilder.build('pimee-proposal-index')
                        .then(function (form) {
                            form.setElement('#enrich-proposal').render();
                        });
                });
            });
    </script>
{% endblock %}
