<dl>
    {% for attribute, change in value['values'] %}
        <dt>{{ present_proposal_attribute(change, attribute) }}</dt>
        <dd>{{ present_proposal_change(change) }}</dd>
    {% endfor %}
</dl>
