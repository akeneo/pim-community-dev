{% oro_title_set({ params: {"%job.label%": form.vars.value.label } }) %}

{% set actionRoute = form.vars.value.id ?
    path('pim_importexport_import_edit', { 'id': form.vars.value.id }) :
    path('pim_importexport_import_create', { 'connector': jobInstance.connector, 'alias': alias }) %}

{% set entityName = 'import profile' %}
{% set title = form.vars.value.id ? 'import profile.edit'|trans ~ ' - ' ~ form.vars.value.label : 'import profile.create'|trans %}

{% set indexRoute = path('pim_importexport_import_index') %}
{% if form.vars.value.id %}
    {% set removeRoute = path('pim_importexport_import_remove', { 'id': form.vars.value.id }) %}
    {% set importRoute = path('pim_importexport_import_index') %}
    {% set removeMessage = 'confirmation.remove.import profile'|trans({ '%name%': form.vars.value.label }) %}
    {% set removeSuccessMessage = 'flash.import.removed'|trans %}
{% endif %}

{% include 'PimImportExportBundle:JobInstance:edit.html.twig' %}
