pim_table_configuration:
  validation:
    table_configuration:
      must_be_filled: Die Tabellenattributkonfiguration muss ausgefüllt werden
      must_contain_at_least_two_columns: Das Tabellenattribut muss mindestens 2 Spalten enthalten
      max_columns_reached: Sie haben die maximale Anzahl an Spalten in Ihrer Tabelle erreicht (10)
      missing_column: Die {{ field }} Spalte muss ausgefüllt werden
      missing_option_field: Das {{ field }} ist erforderlich
      column_code_must_be_filled: Der Spaltencode muss ausgefüllt werden
      option_code_must_be_filled: Der Optionscode muss ausgefüllt werden
      invalid_code: Der Spaltencode darf nur Buchstaben, Zahlen und Unterstriche enthalten
      not_available_code: Dieser Spaltencode ist nicht verfügbar
      invalid_option_code: Der Optionscode darf nur Buchstaben, Zahlen und Unterstriche enthalten
      too_long_code: "Der Spaltencode ist zu lang: Er darf maximal 100 Zeichen lang sein"
      too_long_label: "Die Spaltenbezeichnung ist zu lang: Es muss 250 oder weniger Zeichen haben"
      too_long_option_code: "Der Optionscode ist zu lang: Er darf maximal 100 Zeichen lang sein"
      too_long_option_label: "Die Optionsbezeichnung ist zu lang: Er darf 255 oder weniger Zeichen haben"
      duplicated_column_code: '[0,1]Jede Spalte benötigt einen eindeutigen Code. "%duplicateCodes%" wird bereits verwendet|]1,Inf[Jede Spalte benötigt einen eindeutigen Code. "%duplicateCodes%" werden bereits verwendet'
      column_type_must_be_a_string: Der Spaltendatentyp muss ein String sein
      unknown_data_type: 'Der Spaltendatentyp ist unbekannt. Bitte wählen Sie einen der folgenden: {{ allowed_data_types }}'
      column_label_must_be_a_string: Die Spaltenbezeichnung muss ein String sein
      option_label_must_be_a_string: Die Optionsbezeichnung muss ein String sein
      label_locale_does_not_exist_or_is_not_activated: Das "{{ locale_code }}" Gebietsschema existiert nicht oder ist nicht aktiviert
      invalid_labels_format: Die Spaltenbezeichnungen müssen ein Schlüssel/Werteobjekt sein
      invalid_option_labels_format: Die Optionsbezeichnungen müssen ein Schlüssel/Werteobjekt sein
      must_not_be_filled: Der Typ %type% erlaubt keine table_configuration
      first_column_should_be_valid: '{1}Der erste Spaltentyp sollte immer "{{ allowed_data_types }}" sein, der aktuelle Typ ist "{{ data_type }}". ]1,Inf [Der erste Spaltentyp kann entweder {{ allowed_data_types_except_last }} oder "{{ last_allowed_data_types }}" sein, der aktuelle Typ ist "{{ data_type }}".'
      first_column_code_cannot_be_changed: Der Code Ihrer ersten Spalte kann nicht geändert werden.
      unknown_field: 'Stellen Sie sicher, dass Sie nur erwartete Felder, aktuelles Feld: {{ field }}'
      attribute_does_not_exist: Das Attribut "{{ attribute }}" existiert nicht
      not_table_attribute: Das Attribut "{{ attribute }}" ist kein Tabellenattribut
      column_does_not_exist: Die Spalte "{{ column }}" existiert nicht für das Attribut "{{ attribute }}"
      not_select_column: Die "{{ column }}" Spalte des Attributs "{{ attribute }}" ist kein "select"
      max_should_be_greater_than_min: Der Maximalwert sollte größer als der Minimalwert sein.
      options_cannot_be_set: Optionen können nicht für einen "{{ data_type }}" Spaltentyp gesetzt werden
      reference_entity_identifier_cannot_be_set: Referenz-Entitäts-Kennung kann nicht für einen "{{ data_type }}" Spaltentyp gesetzt werden
      reference_entity_identifier_must_be_filled: Die Referenz-Entitäts-Kennung muss ausgefüllt werden
      reference_entity_identifier_should_exist: Stellen Sie sicher, dass die "{{ reference_entity_identifier }}" Referenz-Entität vorhanden ist, bevor Sie sie zur Tabelle hinzufügen
      reference_entity_identifier_is_immutable: Der reference_entity_identifier kann nicht aktualisiert werden
      no_validations_allowed: 'Keine Validierungen für eine "{{ columnType }}" Spalte verwenden, aktueller Spaltentyp: "{{ given }}"'
      invalid_validation_key: 'Die "{{ given }}" Validierungen funktionieren nicht in einer "{{ columnType }}" Spalte, erlaubt: "{{ expected }}"'
      too_many_table_attributes: Sie haben die maximale Anzahl an Tabellenattributen innerhalb Ihrer PIM erreicht ({{ limit }})
      measurement_family_should_exist: Stellen Sie sicher, dass die "{{ measurement_family_code }}" Messfamilie existiert, bevor Sie sie zur Tabelle hinzufügen
      measurement_unit_should_exist: Stellen Sie sicher, dass die "{{ measurement_unit_code }}" Einheit in der "{{ measurement_family_code }}" Messfamilie vorhanden ist, bevor Sie sie zur Tabelle hinzufügen
      measurement_family_code_must_be_filled: Der Mess-Familiencode muss ausgefüllt werden
      measurement_default_unit_code_must_be_filled: Der Code der Standardmesseinheit muss ausgefüllt werden
      measurement_family_code_cannot_be_set: Der Code der Messfamilie kann nicht für einen "{{ data_type }}"-Spaltentyp festgelegt werden
      measurement_default_unit_code_cannot_be_set: Der Code der Standardmesseinheit kann nicht für einen "{{ data_type }}"-Spaltentyp festgelegt werden
      measurement_family_cannot_be_updated: Der measurement_family_code kann nicht aktualisiert werden
      measurement_family_should_not_be_linked: 'Diese Messfamilie wird in einem Produkt-Attribut verwendet, daher kann sie nicht entfernt werden.'
      measurement_family_unit_should_not_be_linked: 'Diese Messfamilie wird in einem Produkt-Attribut verwendet. Sie können nur die übersetzten Labels und das Symbol einer Einheit bearbeiten.'
    product_value:
      non_existing_columns: '[0,1]Die Spalte "{{ non_existing_columns }}" existiert nicht|]1,Inf[Die "{{ non_existing_columns }}" Spalten existieren nicht'
      unexpected_data_type: 'Die Spalte "{{ columnCode }}" erwartet eine {{ expected }}, {{ given }} gegeben'
      unexpected_data_type_for_measurement: 'Die Spalte "{{ columnCode }}" erwartet einen Code für die Zeichenkette und einen numerischen Betrag, {{ given }} angegeben'
      first_column_required: 'Die Spalte "{{ columnCode }}" ist obligatorisch'
      unknown_option: 'Stellen Sie sicher, dass Sie nur existierende Optionscodes verwenden, den aktuellen Wert: "{{ optionCode }}"'
      integer_required: 'Dieser Wert sollte eine Ganzzahl sein: {{ invalid_value }}'
      min_validation: Dieser Wert sollte {{ limit }} oder mehr betragen.
      max_validation: Dieser Wert sollte kleiner oder gleich {{ limit }} sein.
      value_integer_required: 'Der benötigte Wert ist eine Ganzzahl'
      max_length_validation: Dieser Wert sollte {{ limit }} oder weniger Zeichen enthalten.
      too_many_rows: Sie haben die maximale Anzahl von Zeilen in Ihrer Tabelle erreicht ({{ limit }}).
      record_should_exist: 'Der "{{ recordCode }}" Datensatz in der "{{ referenceEntityIdentifier }}" Referenz-Entität existiert nicht'
      measurement_unit_should_exist: 'Die "{{ measurement_unit_code }}"-Einheit in der "{{ measurement_family_code }}"-Messfamilie existiert nicht'
    product_value_collection:
      too_many_cells: Sie haben die maximale Anzahl an Tabellenzellen in Ihrem Produkt erreicht ({{ count }} Zellen, nur {{ limit }} erlaubt).
    reference_entity:
      should_not_be_linked: 'Diese Referenz-Entität wird in einem Produkt-Attribut verwendet, daher kann es nicht entfernt werden.'
