{% extends 'PimEnrichBundle::layout.html.twig' %}

{% block content %}
    <div id="settings-family">
    </div>

    <script>
        require(
            [
                'jquery',
                'pim/form-builder',
                'pim/fetcher-registry'
            ],
            function (
                $,
                FormBuilder,
                FetcherRegistry
            ) {
                FetcherRegistry.initialize().done(function () {
                    FormBuilder.build('pim-family-index')
                        .then(function (form) {
                            form.setElement('#settings-family').render();
                        });
                });
            });
    </script>
{% endblock %}
