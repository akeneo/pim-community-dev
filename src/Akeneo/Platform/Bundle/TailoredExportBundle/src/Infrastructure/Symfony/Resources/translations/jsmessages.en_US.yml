batch_jobs.xlsx_tailored_product_export.label: Tailored product export in XLSX

akeneo.tailored_export:
  filters:
    completeness:
      locales:
        label: Define the locale for the completeness
        placeholder: Select some locales
        remove: Remove the locale
  form:
    job_instance:
      tab:
        content:
          title: Filter the data
        column:
          title: Select the columns
  column_list:
    title: Columns
    header:
      column_name: Column name
      source_data: Source Data
    column_row:
      target_placeholder: Define your new column name here
      remove: Remove column
      no_source: No source
    no_column_selection:
      title: No columns selection to export
      subtitle: To know more about mapping and operations,
      link: this article may help you
      add_column: Add first column
  column_details:
    sources:
      title: Sources
      add: Add source
      no_result: Sorry, there is no results.
      locale_specific.info: This attribute is locale specific
      remove:
        title: source
        text: Are you sure you want to remove this source?
        button: Remove this source
      no_source_configuration:
        title: There are no operations available for this source. Only its value will be exported.
      no_source_selected:
        title: No source selected for the moment.
        subtitle: To know more about mapping and operations,
        link: this article may help you
      no_column_selected:
        title: No column selected for the moment.
      selection:
        title: Selection
        separator: Separator
        type:
          amount: Amount
          currency: Currency
          key: Filekey
          path: Path
          name: Filename
        format: Format
        parent:
          info:
            locale: In case of the attribute used as label is localisable, the locale value to select
            channel: In case of the attribute used as label is scopable, the channel value to select
  column:
    delete_message: Are you sure you want to remove this column?
  validation:
    columns:
      max_column_count_reached: You reached the maximum column count of {{ limit }}
      target:
        should_not_be_blank: Column name should not be blank
        max_length_reached: Column name is too long. It should have {{ limit }} characters or less.
        should_be_unique: Column name should be unique
    sources:
      max_source_count_reached: You reached the maximum source count of {{ limit }}
    locale:
      should_be_active: Locale {{ locale_code }} should be active
    channel:
      should_exist: Channel {{ channel_code }} should exist
