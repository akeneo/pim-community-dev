{
    "$id": "https://example.com/product",
    "$schema": "https://api.akeneo.com/mapping/product/0.0.12/schema",
    "description": "The data (boolean) must match the type: string at #/properties/size/enum/3",
    "properties": {
        "uuid": {
            "title": "Product uuid",
            "type": "string"
        },
        "colors": {
            "type": "array",
            "items": {
                "type": "number",
                "enum": ["Red", "Green", "Blue"]
            }
        }
    }
}
