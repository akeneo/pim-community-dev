{% if has_workflows(oro_class_name(entity)) %}
    {{
        oro_widget_render({
            'widgetType': 'buttons',
            'url': path('oro_workflow_widget_buttons_entity', {
                entityClass: oro_class_name(entity),
                entityId: entity.id
            }),
            'alias': 'workflow_entity_buttons'
        })
    }}
{% endif %}
