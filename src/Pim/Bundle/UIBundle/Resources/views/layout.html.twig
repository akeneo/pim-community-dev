<!DOCTYPE html>
<html>
<head>
    {% block head %}
        <title>{% block title %}Dashboard{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script type="text/javascript"> localStorage.clear();</script>
        <link rel="stylesheet/less" href="{{ asset('bundles/oroui/css/less/main.less') }}" media="all" />
        <script src="{{ asset('bundles/oroui/lib/less-1.3.1.min.js') }}"></script>
        <link href="{{ asset('bundles/oroui/css/all.css') }}" rel="stylesheet" media="all">
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
        <script type="text/javascript" src="{{ asset('bundles/oroui/lib/bootstrap.min.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/oroui/js/layout.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/bapui/js/jquery-ui-1.9.1.custom.min.js') }}"></script>
    {% endblock %}
</head>

<body>

<div id="page">
    <div id="top-page">

        {% block header %}
        <header>

            <div class="switch-language" style="float:right;">
                {% set route_params = app.request.attributes.get('_route_params') %}
                {% if route_params == "" %}
                    {% set route_params = [] %}
                {% endif %}
                <a href="{{ path(app.request.attributes.get('_route'), route_params | merge({"_locale": "en"})) }}">EN</a>
                &nbsp;-&nbsp;
                <a href="{{ path(app.request.attributes.get('_route'), route_params | merge({"_locale": "fr"})) }}">FR</a>
            </div>

            <h1>Akeneo</h1>

            <div class="navbar">
                <div class="navbar-inner">
                    <a class="brand" href="{{ path('pim_dashboard_default_index') }}">
                        {{ 'Dashboard' | trans }}
                    </a>
                    <ul class="nav">
                        <li class="active"><a href="#">Active link</a></li>
                        <li><a href="#">Link</a></li>
                        <li><a href="#">Link</a></li>
                    </ul>
                </div>
            </div>
        </header>
        {% endblock header %}

        {% block contentwrapper %}
        <div class="container-fluid">

            {% block content %}
            {% endblock %}

        </div>
        {% endblock contentwrapper %}
    </div>
</div>

</body>
</html>