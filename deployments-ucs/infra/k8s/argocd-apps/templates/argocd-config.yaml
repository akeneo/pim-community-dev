apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-config
  namespace: {{ .Values.spec.destination.namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infra
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    path: {{ .Values.spec.source.basePath }}/argocd-config
    targetRevision: {{ .Values.spec.source.targetRevision }}
    helm:
      releaseName: argocd-config
      parameters:
      - name: global.publicDomain
        value: {{ .Values.spec.destination.publicDomain }}
      - name: global.clusterRegion
        value: {{ .Values.spec.destination.clusterRegion }}
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: argocd
