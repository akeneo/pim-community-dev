{
    "name": "@akeneo/connectivity-connection",
    "version": "1.0.0",
    "license": "OSL-3.0",
    "main": "src/index.ts",
    "scripts": {
        "lint": "eslint --config .eslintrc.json --quiet {src,tests}/**/*.{ts,tsx} && prettier --config .prettierrc.json {src,tests}/**/*.{ts,tsx}",
        "test:unit:run": "jest --config jest.config.js"
    },
    "private": true,
    "devDependencies": {
        "eslint": "^8.7.0",
        "jest": "^26.0.0",
        "prettier": "^2.5.1"
    }
}
