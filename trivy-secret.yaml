allow-rules:
  - id: fake-gcloud-json
    description: Skip GCP fake credentials
    path: ^(grth/|tria/|srv/pim/|)config/fake_credentials_gcp.json$
  - id: fake-docker-server-key
    description: Fake docker server private key
    path: ^(tria/|grth/|)docker/server.key$
disable-rules:
disable-allow-rules:
rules:
