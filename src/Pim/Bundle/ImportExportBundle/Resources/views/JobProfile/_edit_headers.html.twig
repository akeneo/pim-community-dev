{% set title =  (entityName ~ '.edit')|trans ~ ' - ' ~ jobInstance.label ~ ' [' ~ jobInstance.code ~ ']' %}

{% set left %}
    <span class="AknTitleContainer-metaItem">{{ 'pim_import_export.job'|trans }}: {{ job.name|trans }}</span>
    <span class="AknTitleContainer-metaItem">{{ 'pim_import_export.connector'|trans }}: {{ jobInstance.connector|trans }}</span>
{% endset %}

{% set titleButtons %}
    {{ elements.backLink(indexRoute) }}
{% endset %}

{% set buttons %}
    {{ elements.deleteLink(removeRoute, removeAcl, indexRoute, removeMessage, removeSuccessMessage, '', 'AknButtonsList-item') }}
    {{ elements.submitBtn('', 'ok', 'AknButtonsList-item') }}
{% endset %}

{{ elements.page_header(title, buttons, null, left, elements.updated(form.vars.id), titleButtons) }}
