services:
    # Normalizers
    pim_versioning.serializer.normalizer.label_translation:
        class: '%pim_serializer.normalizer.label_translation.class%'
        tags:
            - { name: pim_versioning.serializer.normalizer, priority: 90 }
