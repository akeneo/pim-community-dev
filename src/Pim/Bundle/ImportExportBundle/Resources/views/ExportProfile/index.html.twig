{% extends 'PimEnrichBundle::layout.html.twig' %}

{% block content %}
    <div id="export-profile">
    </div>
{% endblock %}

{% block head_script %}
    {{ parent() }}
    <script>
        require(
            [
                'jquery',
                'pim/form-builder'
            ],
            function (
                $,
                FormBuilder
            ) {
                $(function () {
                    FormBuilder.build('pim-export-profile-index')
                        .then(function (form) {
                            form.setElement('#export-profile').render();
                        });
                })
            });
    </script>
{% endblock %}
