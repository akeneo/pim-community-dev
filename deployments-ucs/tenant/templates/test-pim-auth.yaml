{{ if .Values.pim.hook.tests.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-test-pim-auth"
  labels:
    app.kubernetes.io/component: test-pim-auth
    {{- include "pim.standardLabels" . | indent 4 }}
    {{- include "pim.extraLabels" . | indent 4 }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "102"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      labels:
        component: "test-pim-auth" # Deprecated. Cf https://www.notion.so/akeneo/Tagging-strategy-782b4ae037c44d4884b02c8c62e81117
        app.kubernetes.io/component: "test-pim-auth"
        {{- include "pim.standardLabels" . | indent 8 }}
        {{- include "pim.extraLabels" . | indent 8 }}
    spec:
      initContainers:
        - name: wait-dns
          image:  {{ template "pim.imageNameAndTag" . }}
          imagePullPolicy: {{ .Values.image.pim.pullPolicy | quote }}
          command: ["sh", "/scripts-dir/wait-dns.sh"]
          volumeMounts:
            - name: pim-scripts-configmap
              mountPath: /scripts-dir
          env:
            - name: PFID
              value: {{ required ".Values.common.pimMasterDomain" .Values.common.pimMasterDomain }}
      containers:
      - name: test-pim-auth
        image: {{ .Values.image.curl.repository }}:{{ .Values.image.curl.tag }}
        env:
          - name: TARGET
            value: https://{{ required ".Values.common.pimMasterDomain" .Values.common.pimMasterDomain }}
          - name: LOGIN
            value: {{ .Values.pim.defaultAdminUser.login }}
          - name: PASSWORD
            value: {{ .Values.pim.defaultAdminUser.password }}
        command: ["sh", "/scripts-dir/test-pim-auth.sh"]
        volumeMounts:
        - name: pim-scripts-configmap
          mountPath: /scripts-dir
      restartPolicy: Never
      volumes:
      - name: pim-scripts-configmap
        configMap:
          name:  pim-scripts-configmap
{{- end }}
