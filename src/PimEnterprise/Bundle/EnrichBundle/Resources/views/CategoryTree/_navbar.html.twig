{% if form.vars.value.id %}
    {% set tabs = ['Properties']%}
    {% if resource_granted('pimee_enrich_category_edit_permissions') %}
        {% set tabs = tabs|merge(['Permissions']) %}
    {% endif %}
    {% set tabs = tabs|merge(['History']) %}
{% elseif form.vars.value.id %}
    {% set tabs = ['Properties', 'History']%}
{% else %}
    {% set tabs = 'Properties'%}
{% endif %}

{{ elements.form_navbar(tabs) }}
