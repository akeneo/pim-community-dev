knp_gaufrette:
    adapters:
        pim_adapter:
            local:
                directory: %upload_dir%
                create:    true
    filesystems:
        pim:
            adapter: pim_adapter
            alias:   pim_filesystem
