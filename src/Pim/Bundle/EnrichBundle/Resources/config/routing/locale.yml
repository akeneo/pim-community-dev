pim_enrich_locale_index:
    path: /
    defaults: { _controller: pim_enrich.controller.locale:indexAction }

pim_enrich_locale_rest_index:
    path: /rest
    defaults: { _controller: pim_enrich.controller.rest.locale:indexAction }
    methods: [GET]
