workflows:
  raccoon_pull_request:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - ready_to_build?:
          type: approval
          filters:
            branches:
              only:
                - /RAB-.*/
                - /RAC-.*/
      - checkout:
          requires:
            - ready_to_build?
      - build_srnt_dev:
          requires:
            - checkout
      - test_front_tailored_import:
          requires:
            - build_srnt_dev
      - test_back_tailored_import:
          requires:
            - build_srnt_dev
      - pull_request_success:
          requires:
            - test_front_tailored_import
            - test_back_tailored_import
