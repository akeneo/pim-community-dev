executor-machine: &executor-machine

jobs:
    test_back_onboarder:
        machine:
            image: *executor-machine
            docker_layer_caching: true
        steps:
            -   attach_workspace:
                    at: ~/
            -   run:
                    name: Create Behat acceptance output directory
                    command: mkdir -p var/tests/behat/onboarder-serenity-acceptance ~/.cache/yarn
            -   run:
                    name: Change owner on project dir (docker needs uid 1000, circleci can be another uid)
                    command: |
                        sudo chown -R 1000:1000 ../project
            -   run:
                    name: Load archived docker PHP image
                    command: docker load -i php-pim-image.tar
            -   run:
                    name: Clear Docker network
                    command: docker network prune -f
            -   run:
                    name: Start containers
                    command: |
                        APP_ENV=test C='fpm mysql elasticsearch object-storage pubsub-emulator' make up
                        docker/wait_docker_up.sh
            -   run:
                    name: Install database
                    command: APP_ENV=test make database
            -   run:
                    name: Run tests for Onboarder Serenity
                    command: PIM_CONTEXT=onboarder-serenity make tests-back-onboarder
            - store_test_results:
                path: var/tests/phpunit
    test_front_onboarder:
        machine:
            image: *executor-machine
            docker_layer_caching: true
        steps:
            -   attach_workspace:
                    at: ~/
            -   run:
                    name: Create Yarn cache directory
                    command: mkdir -p ~/.cache/yarn
            -   run:
                    name: Change owner on project dir and yarn cache (docker needs uid 1000, circleci can be another uid)
                    command: |
                        sudo chown -R 1000:1000 ../project
                        sudo chown -R 1000:1000 ~/.cache/yarn
            -   run:
                    name: Recreate Docker network
                    command: |
                        docker network prune -f
                        docker network create project_pim
            -   run:
                    name: Pull docker image for node
                    command: docker-compose pull node
            -   run:
                    name: Run tests front for Onboarder Serenity
                    command: PIM_CONTEXT=onboarder-serenity make tests-front-onboarder
