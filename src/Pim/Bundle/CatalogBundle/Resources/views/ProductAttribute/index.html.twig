{% extends 'PimCatalogBundle::layout.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% set title = 'attribute.overview'|trans %}

{% block content %}
    {% set buttons %}
        {% if resource_granted('pim_catalog_attribute_create') %}
            {{ elements.createBtn('attribute', path('pim_catalog_productattribute_create')) }}
        {% endif %}
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    {{ dataGrid.renderGrid('attribute-grid', { localeCode: localeCode }) }}
{% endblock %}
