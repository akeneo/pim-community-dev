apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kube-janitor.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "kube-janitor.labels" . | nindent 4 }}
data:
  rules.yaml: |
    rules:
      {{- .Values.rules | toYaml | trim | nindent 4 }}
