{% extends 'PimEnterpriseWorkflowBundle:MassEditAction:published/configure.html.twig' %}

{% block head_script %}
    {{ parent() }}
    {% if itemsCount == 0 %}
        <script type="text/javascript">
            require(
                ['jquery'],
                function($){
                    'use strict';
                    $(function() {
                        $('a.next').hide();
                    });
                }
            );
        </script>
    {% endif %}
{% endblock %}

{% block formContent %}
    {% if itemsCount == 0 %}
        {% set alertClass = 'error' %}
        {% set alertMessage = 'pim_enrich.mass_edit_action.unpublish.message.error'|trans %}
    {% else %}
        {% set alertClass = 'info' %}
        {% set alertMessage = 'pim_enrich.mass_edit_action.unpublish.message.info'|trans({'%nbProducts%': itemsCount}) %}
    {% endif %}

    <div class="alert alert-{{ alertClass }}">
        <div class="message">
            <ul><li>{{ alertMessage }}</li></ul>
        </div>
    </div>

{% endblock %}
