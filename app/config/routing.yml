_welcome:
    pattern:  /{_locale}
    defaults: { _controller: PimDashboardBundle:Default:index, _locale: %locale% }

pim_dashboard:
    resource: "@PimDashboardBundle/Controller/"
    type:     annotation
    prefix:   /{_locale}/dashboard/
    defaults: { _locale: %locale% }

pim_catalogtaxinomy:
    resource: "@PimCatalogTaxinomyBundle/Controller/"
    type:     annotation
    prefix:   /{_locale}/catalogtaxinomy/
    defaults: { _locale: %locale% }

pim_flexible_product:
    resource: "@PimFlexibleProductBundle/Controller/"
    type:     annotation
    prefix:   /{_locale}/flexibleproduct/
    defaults: { _locale: %locale% }