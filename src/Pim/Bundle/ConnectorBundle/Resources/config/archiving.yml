parameters:
    pim_connector.archive_storage.class: Pim\Component\Connector\ArchiveStorage

services:
    pim_connector.archive_storage:
        class: '%pim_connector.archive_storage.class%'
        arguments:
            - '@oneup_flysystem.archivist_filesystem'
