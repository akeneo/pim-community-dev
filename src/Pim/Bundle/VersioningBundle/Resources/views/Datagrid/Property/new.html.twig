<ul>
{% for fieldKey, fieldValue in value %}
    {% if fieldKey == 'auditData' %}
        {% if fieldValue.new|length %}
        {% for collKey, collValue in fieldValue.new %}
    <li>
        <strong>{{ collKey }}:</strong> {{ collValue|attribute_option_presenter(collKey)|escape }}
    </li>
        {% endfor %}
        {% endif %}
    {% else %}
    <li>
        <strong>{{ fieldKey }}:</strong>
        {{ fieldValue.new|attribute_option_presenter(fieldKey)|escape }}
    </li>
    {% endif %}
{% endfor %}
</ul>
