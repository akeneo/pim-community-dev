<div class="tab-pane tab-history {{ viewElement.loop.first ? 'active' : '' }}" id="{{ viewElement.alias|replace({'.': '-'}) }}" data-url="{{ path('pim_enrich_family_history', {id: form.vars.value.id}) }}"></div>
