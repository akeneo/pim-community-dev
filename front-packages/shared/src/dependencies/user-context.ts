/* istanbul ignore file */

import {UserContext} from '../DependenciesProvider.type';
// @ts-ignore
import legacy from '../../legacy/user-context';

const userContext: UserContext = legacy;

export {userContext};
