apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "timmy.configMapScriptsName" . }}
  labels:
    {{- include "timmy.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
data:
{{ (.Files.Glob "scripts/*").AsConfig | indent 2 }}
