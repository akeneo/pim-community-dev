{% extends 'PimCatalogBundle::layout.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}

    {% set title = 'variant group.overview'|trans %}

    {% set buttons %}
        {{ elements.createBtn(
            'variant group',
            path('pim_catalog_variant_group_create'),
            'dialog',
            'pim_catalog_group_create'
        ) }}
    {% endset %}

    {{ elements.page_header(title, buttons) }}

    {{ dataGrid.renderGrid('variant-group-grid') }}

{% endblock %}
